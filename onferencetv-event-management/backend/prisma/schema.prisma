generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://neondb_owner:npg_qQ7SBcjIO3Zg@ep-blue-band-aiuzrfyo.c-4.us-east-1.aws.neon.tech/neondb?sslmode=require"
}

model Event {
  id                 String   @id @default(cuid())
  eventName          String
  eventDate          DateTime
  speakerName        String
  speakerDesignation String
  description        String?
  speakerIntro       String?
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt
}
